<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Карточка поста (повторно используем item_post.xml) -->
        <include layout="@layout/item_post" />

        <!-- ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ (ДОБАВЛЕНО ПО ТЗ) -->

        <!-- Упомянутые пользователи -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="@string/mentioned_users"
            android:textStyle="bold"
            android:textSize="16sp" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view_mentioned_users"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:visibility="gone" />

        <TextView
            android:id="@+id/text_view_no_mentioned_users"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="@string/no_mentioned_users"
            android:visibility="gone" />

        <!-- Карта с маркером (если есть координаты) -->
        <TextView
            android:id="@+id/text_view_map_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="@string/location"
            android:textStyle="bold"
            android:textSize="16sp"
            android:visibility="gone" />

        <FrameLayout
            android:id="@+id/layout_map_container"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:layout_marginTop="8dp"
            android:visibility="gone">

            <!-- Карта будет добавлена программно -->
            <TextView
                android:id="@+id/text_view_no_map"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:text="@string/no_location"
                android:visibility="gone" />

        </FrameLayout>

    </LinearLayout>

</ScrollView>